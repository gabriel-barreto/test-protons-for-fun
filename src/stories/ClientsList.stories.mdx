import { ThemeProvider } from 'styled-components';
import { BrowserRouter } from 'react-router-dom';
import { Meta, Preview, Story, Wrapper } from '@storybook/addon-docs/blocks';
import { linkTo } from '@storybook/addon-links';

import { StoryLink } from './utils';
import { Client as ClientFactory } from './factories';

import ClientCard from '../components/ClientCard';
import ClientsList from '../components/ClientsList';

import { Theme } from '../styles';

<Meta title="Clients List" component={ClientsList} />

# Clients List

Render a list of `Client Card` components.

### The client card component

<Wrapper>
  <StoryLink onClick={linkTo("Client Card", "Default")}>
    Ir para Client Card stories
  </StoryLink>
</Wrapper>

<Preview>
  <ThemeProvider theme={Theme}>
    <BrowserRouter>
      <ClientCard
        company={ClientFactory.single().company}
        email={ClientFactory.single().email}
        id={ClientFactory.single().id}
        name={ClientFactory.single().name}
      />
    </BrowserRouter>
  </ThemeProvider>
</Preview>

## Props
`clients`: A list of Clients
```typescript
interface Client {
  company: string
  email: string
  id: string
  name: string
}

var clients: Array<Client>
// default value is set to an empty array
```

<Preview>
  <Story name="Default">
    <ClientsList clients={ClientFactory.list(16)} />
  </Story>
</Preview>
